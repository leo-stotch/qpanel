services:
  qpanel:
    # build: .
    image: ghcr.io/leo-stotch/qpanel:sha-b82e561
    container_name: qpanel
    ports:
      - "${FLASK_PORT:-5001}:5001"
    volumes:
      - qpanel-data:/app/data
      # - /media/downloads/:/downloads:rw
    restart: unless-stopped

volumes:
  qpanel-data:
