services:
  qpanel:
    # build: .
    image: ghcr.io/leo-stotch/qpanel:sha-d7dd99b
    container_name: qpanel
    ports:
      - "${FLASK_PORT:-5001}:5001"
    volumes:
      - qpanel-data:/app/data
      # - /media/downloads/:/downloads:rw
    restart: unless-stopped

volumes:
  qpanel-data:
