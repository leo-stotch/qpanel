services:
  qpanel:
    # build: .
    image: ghcr.io/leo-stotch/qpanel:sha-2eb2102
    container_name: qpanel
    ports:
      - "${FLASK_PORT:-5001}:5001"
    volumes:
      - ./data:/app/data
      # - /media/downloads/:/downloads:rw
    restart: unless-stopped
